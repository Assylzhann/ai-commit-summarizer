Install dependencies

pip install -r requirements.txt


Set your API key

export OPENAI_API_KEY="sk-..."


Make the script executable

chmod +x ai_commit.py


Add a Git alias

git config alias.aic "!python3 ai_commit.py"


Now you can run:

git add .
git aic


→ AI will automatically generate a meaningful commit message from your diff
